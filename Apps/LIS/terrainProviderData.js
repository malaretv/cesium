// Keep this in its own file in order to avoid errors when importing it

export let isOptimizedPolarTerrain = false;

export function setOptimizedPolarTerrainEnabled(yes) {
  isOptimizedPolarTerrain = yes;
}
